// valid keyword have to pass this final filteration rules
const rules = {
  alphabets: true,
  numbers: '0-9',
  spaces: false,
  symbols: '"" \'',
  minlength: '2',
  maxlength: ''
};


export default rules;
